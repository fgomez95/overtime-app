<tr>
    <td><%= post.id %></td>
    <td><%= truncate(post.rationale) %></td>
    <td><%= post.date %></td>
    <td><%= post.user.full_name %></td>
    <td><%= link_to "Edit", edit_post_path(post), id: "post-#{post.id}" %></td>
</tr>
